export const ipconfig = {
    ws_url: "http://IPADDRESS:5000"
};

//This is where you'll have to update your IPdress to run the application. It won't work on the schools wifi though. Make sure
// this file is in the gitignore before updating so your ip address isn't pushed to git. 